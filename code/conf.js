const appConfig = Object.freeze({
    server: {
        port: 3000,
    },
    assets: {
        favicon: "favicon.png",
        stylesheet: "styles/index.css",
        momentScript: "https://cdnjs.cloudflare.com/ajax/libs/moment.js/2.29.4/moment-with-locales.min.js",
        appScript: "scripts/index.js",
        themeScript: "scripts/theme.js",
    },
});

export default appConfig;
