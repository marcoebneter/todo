<!doctype html>
<html lang="de">
    <head>
        <meta charset="UTF-8" />
        <meta name="viewport" content="width=device-width, initial-scale=1.0" />
        <title>{{pageTitle}}</title>

        <link rel="icon" href="{{assets.favicon}}" type="image/png" />
        <link rel="stylesheet" href="{{assets.stylesheet}}" />

        <script src="{{assets.momentScript}}"></script>
        <script src="{{assets.appScript}}" type="module" defer></script>
        <script src="{{assets.themeScript}}" type="module" defer></script>
    </head>
    <body>
        {{{body}}}
    </body>
</html>
