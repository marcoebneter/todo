{
    "name": "todo-app",
    "version": "1.0.0",
    "description": "This is a todo app to organize your tasks and increase productivity. It is also the Project 1 of the CAS Frontend Engineering course.",
    "license": "ISC",
    "author": "Marco Ebneter",
    "type": "module",
    "main": "node src/server/server.js",
    "scripts": {
        "start": "node src/server/server.js",
        "dev": "concurrently -k -n BACKEND,VIEW -c cyan,magenta \"npm run backend:watch\" \"npm run frontend:live\"",
        "backend:watch": "nodemon --watch src/server --watch src/client --ext js,mjs,cjs,html,css src/server/server.js",
        "frontend:live": "browser-sync start --proxy \"http://localhost:3000\" --files \"src/client/**/*\" --port 3001 --no-ui",
        "smoke": "node scripts/smoke-test.mjs",
        "lint": "eslint .",
        "lint:fix": "eslint . --fix",
        "lint-staged": "lint-staged",
        "format": "prettier --write \"**/*.{js,cjs,mjs,css,html,json,md}\"",
        "format:check": "prettier --check \"**/*.{js,cjs,mjs,css,html,json,md}\"",
        "verify": "npm run lint && npm run smoke",
        "prepare": "husky"
    },
    "lint-staged": {
        "*.{js,mjs,cjs,css,html,json,md}": "prettier --write",
        "*.{js,mjs,cjs,css,html}": "eslint --fix"
    },
    "devDependencies": {
        "@eslint/css": "^1.1.0",
        "@eslint/js": "^10.0.1",
        "@html-eslint/eslint-plugin": "^0.60.0",
        "@html-eslint/parser": "^0.60.0",
        "browser-sync": "^3.0.3",
        "concurrently": "^9.2.1",
        "eslint": "^10.2.0",
        "globals": "^17.5.0",
        "husky": "^9.1.7",
        "lint-staged": "^17.0.2",
        "nodemon": "^3.1.14",
        "prettier": "^3.8.3"
    },
    "dependencies": {
        "express": "^5.2.1",
        "sqlite3": "^6.0.1"
    }
}
